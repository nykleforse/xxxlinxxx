Версия 1.06.05.26

Что мессенджер умеет сейчас:

- Создание и восстановление аккаунта по фотографии.
- Генерация локального ID пользователя и копирование ID.
- Локальный список контактов с пользовательскими именами.
- Экран личного чата для сохранённых контактов.
- Зашифрованные текстовые сообщения через Firestore.
- Генерация RSA-пары ключей для шифрования сообщений.
- AES/GCM-шифрование содержимого сообщений.
- Резервная доставка текстовых сообщений через Firestore.
- Статус доставки сообщения: одна галочка после отправки.
- Статус прочтения сообщения: две галочки после обработки сообщения получателем.
- Счётчики непрочитанных сообщений по каждому чату в списке контактов.
- Системные уведомления о сообщениях со звуком и вибрацией.
- Уведомления о входящих звонках с поддержкой полноэкранного показа.
- Регистрация Firebase Cloud Messaging токена.
- Firebase Functions для push-уведомлений о сообщениях и звонках.
- Firestore rules для пользователей, сообщений, статусов прочтения, звонков, ICE-кандидатов и фото-аккаунтов.
- WebRTC-сигналинг звонков через Firestore.
- Настройка STUN/TURN-серверов для установки peer-to-peer соединения.
- Голосовые звонки через WebRTC.
- Голосовые режимы: comfy, base и Xtream.
- Нативный мост Codec2 для низкоскоростных голосовых режимов.
- Отключение микрофона во время звонка.
- Переключение аудиовыхода между динамиком и телефоном во время звонка.
- Экран активного звонка с таймером.
- Экраны входящего, исходящего и активного звонка.
- Отклонение, отмена, завершение звонка и обработка таймаута.
- P2P-сообщения через DataChannel при открытом peer-to-peer соединении.
- Прямая передача фотографий с телефона на телефон через WebRTC DataChannel.
- Передача фотографий кусками без хранения фотографии в Firebase.
- Локальное сохранение полученных напрямую фотографий в app-specific Pictures.
- Локальная история чатов на устройстве.
- Файлы агентного workflow для координации проекта.
- Описания агентов: Liaison, Director, Overseer, Android UI, Firebase, WebRTC P2P, Security & Privacy и Build & Regression.

Версия 1.11.05.26

Новые функции:

- *Кнопка выхода для возврата из текущего фото-аккаунта к входу по фотографии.*
- *Более надёжный вход по фотографии на основе визуального отпечатка снимка, а не только байтов файла.*
- *Проверка совместимости с фото-аккаунтами, созданными старым способом входа.*
- *Более безопасное чтение фотографии при входе, чтобы снизить риск вылета или сворачивания после выбора большого изображения.*
- *Повышен Android-номер сборки, чтобы новый APK мог устанавливаться поверх предыдущего.*

Версия v1.12

Новые функции:

- *Стабильная подпись APK для debug- и release-сборок.*
- *Поддержка подписанного release APK для более корректной установки и обновления.*
- *Проект синхронизирован с последней beta-версией GitHub release tag `v1.12`.*
- *Android `versionName` приведён к логике GitHub releases: `1.12-beta`.*
- *Android `versionCode` повышен до `12`, чтобы подписанная beta-сборка устанавливалась поверх предыдущих сборок.*
- *У исходящих сообщений теперь три видимых состояния: ждёт отправки, отправлено и получено.*
- *Текстовые сообщения остаются в очереди и автоматически повторяют отправку при нестабильной сети или VPN.*
- *Восстановлена прямая передача фотографий с телефона на телефон через WebRTC DataChannel без хранения фотографий в Firebase.*

Версия v1.13.2

Новые функции:

- *Android `versionName` обновлён до `1.13.2-beta`.*
- *Android `versionCode` повышен до `132` для beta APK v1.13.2.*
- *APK-файл опубликован в репозитории под именем beta-версии v1.13.2.*

Версия v1.13.3

Новые функции:

- *Android `versionName` обновлён до `1.13.3-beta`.*
- *Android `versionCode` повышен до `133` для beta APK v1.13.3.*
- *APK подписан тем же сертификатом, что и v1.12, чтобы обновляться поверх старых установленных сборок.*

Версия v1.13.4

Новые функции:

- *База релиза исправлена: вместо устаревшей ветки `develop` используется актуальная ветка `main`.*
- *Android `versionName` обновлён до `1.13.4-beta`.*
- *Android `versionCode` повышен до `134`, чтобы устройства могли обновиться с нерабочей сборки v1.13.3.*
- *Конфигурация APK сохраняет поддержку устройств `arm64-v8a` и `armeabi-v7a`.*
- *Подпись релизной сборки использует локальный сертификат, совместимый с v1.12, без сохранения keystore в Git.*

Версия v1.14.11-beta

Новые функции:

- *Восстановлен сигнальный обмен для передачи фотографий, чтобы прямая доставка с телефона на телефон доходила до получателя.*
- *Правила передачи теперь разрешают фото-запрос, ответ, завершение передачи и ICE-кандидаты.*
- *Отправка фотографий через DataChannel теперь ждёт свободный буфер и сообщает о сбое, а не показывает только локальную фотографию.*
- *Android `versionName` обновлён до `1.14.11-beta`.*
- *Android `versionCode` повышен до `151`, чтобы beta-сборка устанавливалась поверх предыдущих версий.*

Версия v1.15.16

Новые функции:

- *Android `versionName` обновлён до `1.15.16`.*
- *Android `versionCode` повышен до `216`, чтобы сборка устанавливалась поверх предыдущих версий 1.15.x.*
- *Прямая передача фото теперь не считает свежий запрос устаревшим, если получатель открыл приложение уже после начала отправки.*
- *Прямая передача фото теперь автоматически принимает фотографии от сохранённых контактов и из уже существующих чатов, не оставляя их зависшими за окном неизвестного отправителя.*
- *Обработка состояний передачи фото теперь разрешает сбой, истечение времени, отказ и занятость, чтобы зависшие передачи корректно отображались и очищались.*

Версия v1.15.19-beta

Новые функции:

- *Android `versionName` обновлён до `1.15.19`.*
- *Android `versionCode` повышен до `219`, чтобы сборка устанавливалась поверх предыдущих версий 1.15.x.*
- *Прямая передача фото теперь ждёт подтверждения от получателя, прежде чем помечать фотографию доставленной.*
- *Получатель теперь отправляет `PHO_ACK` только после локального сохранения фото и `PHO_NACK`, если передача неполная, фото не расшифровалось или не сохранилось.*
- *Передача фото теперь использует тот же набор STUN/TURN-серверов, что и звонки, для более надёжного соединения.*
- *Временные WebRTC-разрывы больше не обрывают активную передачу фото мгновенно.*

Версия v1.15.20-beta

Новые функции:

- *Android `versionName` обновлён до `1.15.20`.*
- *Android `versionCode` повышен до `220`, чтобы сборка устанавливалась поверх предыдущих версий 1.15.x.*
- *WebRTC setup для фото теперь пропускает TURN-серверы, если нет TURN-логина и пароля, вместо передачи невалидных серверов в peer connection.*
- *Передача фото теперь использует более лёгкую отдельную ICE-конфигурацию и повторяет setup через STUN-only, если основной photo peer connection не создался.*
- *При неудачном setup отправка фото теперь останавливается чисто и помечает локальное ожидающее фото как failed, а не продолжает поломанную цепочку.*
- *Залипшие старые photo peer-соединения очищаются перед началом новой передачи фото.*

Следующие версии:

- Добавлять новую главу версии ниже этой.
- Сначала указывать базовые возможности.
- В следующих версиях перечислять только новые функции и выделять новые пункты курсивом.
- Всегда обновлять `en_Version.txt` и `rus_Version.txt` вместе.
- Не редактировать старые главы версий, кроме исправления опечаток.

Шаблон следующей версии:

Версия v1.13

Новые функции:

- *Новая функция.*

